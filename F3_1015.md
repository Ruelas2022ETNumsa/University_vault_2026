# Ejercicio 3 — Sistema de tiempo discreto recursivo

Sistema recursivo con entrada $x(n) = \delta(n)$ y condición inicial $y(-1) = 0$. Determinar $y(n)$ para $0 \le n \le 5$ y graficar la respuesta.

$$
y(n) = 2y(n-1) + x(n), \qquad x(n) = \delta(n), \qquad y(-1) = 0
$$

---

## Resolución analítica

### Desarrollo muestra a muestra

$$
x(n) = \begin{cases} 1, & n = 0 \\ 0, & n \neq 0 \end{cases}
$$

| $n$ | $y(n) = 2y(n-1) + x(n)$ | $y(n)$ |
|:---:|:---|:---:|
| 0 | $2(0) + 1$ | **1** |
| 1 | $2(1) + 0$ | **2** |
| 2 | $2(2) + 0$ | **4** |
| 3 | $2(4) + 0$ | **8** |
| 4 | $2(8) + 0$ | **16** |
| 5 | $2(16) + 0$ | **32** |

Forma general:

$$
\boxed{y(n) = 2^n\, u(n)}
$$

### Análisis de estabilidad

Transformada Z de la ecuación en diferencias (condiciones de reposo):

$$
Y(z)(1 - 2z^{-1}) = X(z) \implies H(z) = \frac{Y(z)}{X(z)} = \frac{z}{z-2}
$$

Polo del sistema: $z_p = 2$. Como $|z_p| = 2 > 1$, el polo cae fuera del círculo unitario y la ROC del sistema causal ($|z| > 2$) no incluye $|z| = 1$.

**El sistema es inestable BIBO** — la salida crece exponencialmente ante entrada acotada.

---

## Código MATLAB

```matlab
% ETN1015 — Ejercicio 3: Sistema recursivo inestable
% Ref: Palani Cap. 1 · Oppenheim EN (6to11) Cap. 6 · Ingle & Proakis Cap. 2
clear; clc; close all;

N = 6;
x = zeros(1, N);  x(1) = 1;   % x(n) = delta(n)
y = zeros(1, N);
y_prev = 0;                    % y(-1) = 0

for n = 1:N
    if n == 1
        y(n) = 2*y_prev + x(n);
    else
        y(n) = 2*y(n-1) + x(n);
    end
end

n_vec = 0:5;
figure(1);
stem(n_vec, y, 'filled', 'LineWidth', 1.5, 'Color', [0.8500 0.3250 0.0980]);
xlabel('n'); ylabel('y(n)');
title('y(n) = 2y(n-1) + \delta(n)  —  polo en z = 2 (inestable)');
xlim([-1, 6]); ylim([0, max(y)+5]);
grid on;
```
