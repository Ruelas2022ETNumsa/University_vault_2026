Resolvé con MATLAB: Convolución circular de dos secuencias (x2 modificada).

Dado:
x1(n) = {2, 1, 2, 1} (origen en n=0)
x2(n) = {1, 2, 3, 4, 5} (origen en n=0)
N = 5 (longitud de la secuencia más larga — x1 se completa con ceros)

Calculá x3(m) = x1(n) ⊛ x2(n) usando el método de la matriz de convolución circular.
Verificá el resultado graficando x3(m) con stem.
